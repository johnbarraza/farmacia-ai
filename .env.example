# SaludApp Peru — Variables de Entorno
# Copiar a .env y llenar las keys. NUNCA subir .env a GitHub.

# Claude AI / Anthropic (OCR Vision + extracción de medicamentos)
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini (fallback OCR Vision)
GEMINI_API_KEY=AIza...

# DeepSeek (fallback razonamiento texto)
DEEPSEEK_API_KEY=sk-...

# Backend URL (para Streamlit → FastAPI)
BACKEND_URL=http://localhost:8000
